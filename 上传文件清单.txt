上传时请把本文件夹里的全部内容一起拖到 GitHub upload/main 页面。
必须包含隐藏文件夹 .github，否则每日热点不会自动更新。
必须包含 scripts 文件夹，否则 workflow 会找不到抓取脚本。
必须包含 data/hotspots.json，否则前端没有热点数据文件。
上传后到 Actions -> Update Daily Hotspots -> Run workflow 手动运行一次验证。
